{
  "name": "botpress-node-modules-workaround",
  "version": "1.0.0",
  "description": "Hack needed because bp binary recursively reads parent dir for node_modules folder and attempts to use libraries.",
  "main": "index.js",
  "dependencies": {
    "nanoid": "^3.3.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
